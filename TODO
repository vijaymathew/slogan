02-Jan-2016
===========

  * Update all ffi_call_*** functions to take this function handle instead of function name.
  * Test the `declare ffi` extension - slogan_obj->c_struct conversion,
    test all types in function arguments, return values and struct members.
  * Test the package manager for git packages.
