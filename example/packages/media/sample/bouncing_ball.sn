let m = media

let x, y, xspeed, yspeed = 100, 100, 1, 3
let width, height = 640, 360
let fg = #u8[175, 0, 0, 255]

function draw(c)
{ x = x + xspeed
  y = y + yspeed

  when ((x > width) || (x < 0)) xspeed = -xspeed
  when ((y > height) || (y < 0)) yspeed = -yspeed
  task_sleep(.005)
  m.filled_ellipse(c, x, y, 16, 16, fg) }

m.display(draw, w = width, h = height)
  