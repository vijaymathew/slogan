function http_server() 
  handler(open_tcp_server_stream(8080));

function handler(server) 
  let (client = read(server)) 
    { display("hello, world", client);
      close_stream(client);
      handler(server) };

http_server();