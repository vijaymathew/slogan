function http_server() 
  handler(tcp_server_stream(7000));

function handler(server) 
  let (client = read(server)) 
  { write("hello, world", client);
    close_stream(client);
    handler(server) };

http_server();
